import React from "react";
import ReactDOM from "react-dom/client"; // Make sure this is 'ReactDOM' with a capital "DOM"
import "./style.css"
import App from"./App"



const root = ReactDOM.createRoot(document.getElementById("root")); // Use "root" as a string


root.render(<App></App>);

